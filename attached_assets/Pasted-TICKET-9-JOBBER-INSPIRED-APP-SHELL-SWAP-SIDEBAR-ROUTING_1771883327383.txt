TICKET 9 — JOBBER-INSPIRED APP SHELL SWAP (SIDEBAR) + ROUTING DEFAULT TO /clients

CONTEXT / NON-NEGOTIABLES
- Keep existing auth exactly: PIN login, HMAC-signed HttpOnly cookie, 24h TTL.
- Root / behavior:
  - unauthenticated -> redirect to /login
  - authenticated -> redirect to /clients (Client Book default landing)
- Public /quote stays public and must remain accessible without login.
- Remove “Home” concept. No home button anywhere.
- Keep brand look (black/blue/white). Sidebar + main content must be mobile-friendly.
- Do NOT break existing /crm/* pages yet; just re-skin them under the new shell where applicable.
- Replit can handle large files; do not micro-split files. Prioritize clarity.

GOAL
Replace the current header+horizontal nav layout on authenticated pages with:
- Persistent LEFT SIDEBAR (desktop) + main content
- Sidebar includes module links and a “Create” dropdown UI
- New default authenticated landing page: GET /clients (can be a placeholder page for now)
- Preserve current dashboard route, leads route, etc. (even if they’re still under /crm/*)

DELIVERABLES
1) New shared stylesheet for app shell (sidebar + main)
2) New sidebar HTML snippet used by all authenticated pages
3) New route(s):
   - GET /clients (auth required) -> serves /pages/clients.html
4) Update server routing redirects:
   - GET / -> if authed redirect /clients else /login
   - After successful POST /login -> redirect /clients
5) Remove “Home” link/button from any nav.
6) Ensure sidebar is present on:
   - /clients
   - /crm/dashboard
   - /crm/leads
   - /crm/new
   - /crm/schedule
   - /crm/time
   - /crm/expenses
   - /crm/audit
(If any of these pages are not present yet, just implement for those that exist.)

FILES TO CREATE / MODIFY

A) CREATE: /pages/css/shell.css
Purpose: app shell layout + sidebar styles. Keep it compatible with existing brand.css.
Requirements:
- Desktop:
  - sidebar fixed on left, width ~240px, full height, scrollable if needed
  - main content to the right with padding
- Mobile:
  - sidebar becomes horizontal wrap at top OR collapses into a compact top strip
  - must not require hamburger in Ticket 9
  - content remains readable and functional on iPhone
- Colors: black/white with blue accents. Don’t overdo bright colors.
- Provide classes:
  - .app-shell (overall layout container)
  - .sidebar
  - .sidebar-header (logo/title)
  - .sidebar-nav
  - .sidebar-link (active + hover states)
  - .sidebar-section-title
  - .create-btn
  - .create-menu (hidden by default)
  - .main-area
  - .page-header-row (page title + optional actions like Refresh)

B) CREATE: /pages/partials/sidebar.html
A pure HTML snippet we can inject in each authenticated page.
Include:
- Vickery Electric logo (text-based is fine) + “CRM”
- Create dropdown button
  - Menu items (UI only for now; links can go to existing pages):
    - New Client -> /crm/new (placeholder until real client create exists)
    - New Request -> /crm/new
    - New Quote -> /quote (opens public quote, OK)
    - New Job -> /crm/new (placeholder)
    - Schedule Visit -> /crm/schedule
- Links:
  - Dashboard -> /crm/dashboard
  - Schedule -> /crm/schedule
  - Clients -> /clients
  - Requests -> /crm/leads   (use existing Leads list for now)
  - Quotes -> /crm/leads     (placeholder: same list for now)
  - Jobs -> /crm/schedule    (placeholder)
  - Invoices -> /crm/dashboard (placeholder)
  - Time -> /crm/time
  - Expenses -> /crm/expenses
  - Audit -> /crm/audit
  - Settings -> /crm/dashboard (placeholder)
- Logout -> /logout (bottom of sidebar or last link, smaller text)

C) CREATE: /pages/js/shell.js
Purpose: small JS helpers for shell:
- Toggle Create dropdown open/close
- Close dropdown on outside click
- Highlight active sidebar link based on current path
Implementation notes:
- No frameworks.
- Must not break existing page JS files.
- Use data attributes like data-nav="/clients" to match current route.

D) CREATE: /pages/clients.html
Purpose: placeholder Client Book page (real list in Ticket 11).
Requirements:
- Must be auth-gated (server side).
- Use the new app shell layout.
- Page header “Clients”
- Body card that says:
  - “Client Book coming next (Ticket 11)”
  - Buttons:
    - “Go to Requests (Leads)” -> /crm/leads
    - “Open Dashboard” -> /crm/dashboard
Keep styling consistent.

E) MODIFY: every authenticated HTML page to wrap content in shell layout
For each existing authenticated page HTML in /pages:
- Add link tags:
  - brand.css (existing)
  - shell.css (new)
- Add script:
  - shell.js (new)
- Wrap page body like:

<body>
  <div class="app-shell">
    <!-- Sidebar injected -->
    <div class="sidebar" id="sidebarMount"></div>

    <main class="main-area">
      <div class="page-header-row">
        <h1>...</h1>
        <!-- optional actions -->
      </div>

      <div class="main-content">
        <!-- existing content -->
      </div>
    </main>
  </div>

  <script>
    // load sidebar partial
  </script>
</body>

Sidebar injection approach (simple, no build tools):
- Add a small inline script (or put in shell.js) that fetches /pages/partials/sidebar.html and injects into #sidebarMount.
- Example:
  fetch('/pages/partials/sidebar.html').then(r=>r.text()).then(html=>{ mount.innerHTML = html; initShell(); });

You must ensure initShell() runs after injection.

F) MODIFY: server routes (index.js or server.js)
1) Root redirect logic:
- GET /:
  - if authed -> redirect /clients
  - else -> redirect /login

2) Login success redirect:
- POST /login:
  - if PIN ok, set cookie, redirect /clients

3) Auth gate middleware:
- Apply to /clients and all /crm/* routes.
- Ensure /quote remains public.

4) Add:
- GET /clients -> serve clients.html (protected)

G) REMOVE HOME BUTTON
- If there’s any existing top nav with “Home” remove it.
- If dashboard currently includes a “Home” link, remove it.

ACCEPTANCE TESTS (YOU MUST VERIFY MANUALLY)
1) Open app root:
   - if not logged in -> /login
   - if logged in -> /clients
2) After entering correct PIN -> lands on /clients
3) /quote loads without logging in
4) Sidebar appears on /clients and all existing /crm/* pages
5) No “Home” link anywhere
6) Create dropdown opens/closes reliably on mobile and desktop
7) Active module link highlights properly

OUTPUT REQUIREMENT
- Implement all file changes and provide a short “Changed Files” list at the end.
- Do not introduce any mock data. No API changes beyond routing/auth in Ticket 9.